const _ = require('lodash')

setInterval(() => console.log(_.random(1, 1000)), 1000)

//instalar via npm o lodash e o nodemon
//nodemon serve para dar refresh no codigo em execução assim que houver uma mudança